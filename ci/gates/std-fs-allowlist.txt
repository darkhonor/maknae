# Enrollment is one privileged create/chmod/chown transaction. maknae-io has no
# chown-capable transaction contract; wrapping only the byte write would split it.
bins/maknae/src/enroll/artifact_write.rs:119|std::fs::set_permissions(
bins/maknae/src/enroll/artifact_write.rs:121|std::fs::Permissions::from_mode(artifact.mode),
bins/maknae/src/enroll/artifact_write.rs:130|std::fs::create_dir_all(&artifact.path).map_err(|e| io_err(&artifact.path, e))?;
bins/maknae/src/enroll/artifact_write.rs:140|std::fs::write(&artifact.path, bytes).map_err(|e| io_err(&artifact.path, e))?;
bins/maknae/src/enroll/mod.rs:712|let _ = std::fs::remove_file(&out_path);
bins/maknae/src/enroll/mod.rs:742|let _ = std::fs::remove_file(&out_path); // best-effort cleanup

# Enrollment input reads intentionally preserve their existing caller-selected symlink and
# permission semantics. maknae-io has no follow-final-symlink read contract; migration would
# be a behavior change and belongs to the enrollment I/O contract work, not this storage pass.
bins/maknae/src/enroll/mod.rs:857|let raw = std::fs::read_to_string(path).map_err(|e| EnrollError::Io {
bins/maknae/src/enroll/mod.rs:1205|let vault_ca_bytes = std::fs::read(&vault_ca_path).map_err(|e| EnrollError::Io {
bins/maknae/src/enroll/mod.rs:1221|let text = std::fs::read_to_string(&state_path).map_err(|e| EnrollError::Io {
bins/maknae/src/enroll/mod.rs:1263|Some(root_path) => match std::fs::read_to_string(&root_path) {

# Audit holds one persistent append fd and sync_data()s the hot path. Anchor::append
# opens per call and intentionally has a different lifecycle/durability contract.
crates/maknae-audit-append/src/sink.rs:13|use std::fs::File;
crates/maknae-audit-append/src/sink.rs:19|fn open_options() -> std::fs::OpenOptions {
crates/maknae-audit-append/src/sink.rs:21|let mut opts = std::fs::OpenOptions::new();
crates/maknae-audit-append/src/sink.rs:75|fn open_options() -> std::fs::OpenOptions {
crates/maknae-audit-append/src/sink.rs:76|let mut opts = std::fs::OpenOptions::new();

# The DEFINITIONS of the three named requirement-free identifiers (issue #132). Each
# says "OS DAC is sufficient at this scope" once, in one reviewed place, so a caller
# can no longer express that decision anonymously — the gate inventories the identifier
# as well as the struct-literal fields, and the identifier is what a caller writes.
crates/maknae-io/src/checks.rs:43|pub const OS_DAC: AnchorRequired = AnchorRequired {
crates/maknae-io/src/checks.rs:44|owner: None,
crates/maknae-io/src/checks.rs:45|mode_mask: None,
crates/maknae-io/src/checks.rs:59|pub const OS_DAC: DescendantRequired = DescendantRequired {
crates/maknae-io/src/checks.rs:60|owner: None,
crates/maknae-io/src/checks.rs:61|mode_mask: None,
crates/maknae-io/src/checks.rs:87|pub const OS_DAC_REGULAR: TargetRequired = TargetRequired {
crates/maknae-io/src/checks.rs:88|owner: None,
crates/maknae-io/src/checks.rs:89|mode_mask: None,

# read_absolute pins the configured path's parent; OS DAC is the named directory
# requirement while the caller supplies the target artifact requirements. Named ONCE
# here since issue #137 — maknae-config::loader, maknae-config::authz and maknae-vault
# each carried their own copy of this anchor requirement until the adapter absorbed
# absolutization and they were deleted.
crates/maknae-io/src/anchor.rs:266|let anchor = open_anchor_resolved(parent, AnchorRequired::OS_DAC, pref)?;

# read_storage carries no requirement of its own beyond regular-file: it reads
# non-sensitive artifacts (CA pins, sealed-source blobs), and every caller needing a
# permission gate applies it separately (client::read_secret_credential's 0o077 mask).
crates/maknae-vault/src/lib.rs:90|let target = maknae_io::TargetRequired::OS_DAC_REGULAR;

# owner: None means OS DAC is sufficient for OWNERSHIP at that scope; the mode
# requirement standing beside it is named explicitly, and stronger artifact
# requirements are separately named on the target (authz/posture/plaintext credential
# where applicable). loader.rs:34 is CONFIG_ARTIFACT, the named const that replaced
# `read_secure_required(path, None, Some(0o007))` — the positional spelling issue #132
# reported as invisible to this inventory.
crates/maknae-config/src/loader.rs:34|owner: None,
crates/maknae-config/src/loader.rs:182|owner: None,
crates/maknae-config/src/loader.rs:224|owner: None,
crates/maknae-config/src/loader.rs:270|owner: None,
crates/maknae-vault/src/client.rs:137|owner: None,

# Unix-domain socket lifecycle belongs to transport (#66), not storage I/O.
crates/maknae-vault/src/socket.rs:16|let meta = std::fs::metadata(dir).map_err(|e| VaultError::InsecureSocketDir {
crates/maknae-vault/src/socket.rs:56|match std::fs::symlink_metadata(path) {
crates/maknae-vault/src/socket.rs:75|std::fs::remove_file(path)
crates/maknae-vault/src/socket.rs:107|let _ = std::fs::remove_file(path);
crates/maknae-vault/src/socket.rs:119|std::fs::set_permissions(path, std::fs::Permissions::from_mode(0o660))
crates/maknae-vault/src/stream.rs:189|let _ = std::fs::remove_file(&self.path);
crates/maknae-vault/src/stream.rs:228|let _ = std::fs::remove_file(path);
