//! maknae-mcp — MCP protocol types + client + OAuth client logic (no secret material).
//! Linked by BOTH binaries; maknaed injects the maknaed-only OAuth secret (spec §2.13, §3).
//! SCAFFOLD STUB. Body gated on its component ADR.
pub const CRATE_MARKER: &str = "maknae-mcp";
