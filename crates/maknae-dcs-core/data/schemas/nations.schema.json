{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ISO 3166-1 alpha-3 nations registry",
  "description": "The engine's national world view. Every releasability trigraph must be a member (recognition), else the label is InvalidElement (#26). Enforced structurally by build.rs; a violation fails the build.",
  "type": "object",
  "required": ["version", "source", "codes"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "string", "minLength": 1 },
    "source": { "type": "string", "minLength": 1 },
    "codes": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[A-Z]{3}$"
      }
    }
  }
}
