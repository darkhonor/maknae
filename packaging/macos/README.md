# macOS packaging (stub)
Tool: `pkgbuild` / `productbuild` + launchd plists + `_maknae` daemon user (spec §6).
Signed + notarized when ready. macOS is a dev profile, not an enforcement reference (spec §11 Q4).
Not yet implemented — this deliverable is scaffold structure only.
