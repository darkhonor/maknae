schema_version: 1
permissions:
  allow:
    - "Read(~/**)"
    - "Write(~/projects/**)"
  deny:
    - "Read(~/.ssh/**)"
    - "Read(~/.gnupg/**)"
    - "Read(~/.aws/**)"
    - "Read(~/.vault-token)"
    - "Read(~/.netrc)"
    - "Read(~/.git-credentials)"
    - "Read(~/.kube/**)"
    - "Read(~/.docker/config.json)"
    - "Read(~/.maknae/**)"
    - "Write(~/.ssh/**)"
    - "Write(~/.gnupg/**)"
    - "Write(~/.aws/**)"
    - "Write(~/.vault-token)"
    - "Write(~/.netrc)"
    - "Write(~/.git-credentials)"
    - "Write(~/.kube/**)"
    - "Write(~/.docker/config.json)"
    - "Write(~/.maknae/**)"
